import React from "react";
import ReactDOM from "react-dom/client";
import Resume from "@/entrypoints/resume/Resume.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
    <React.StrictMode>
        <Resume />
    </React.StrictMode>,
);
